<!-- nfr-design stage memory — unit-sla-escalation -->

## Interpretations
- 2026-09-10T15:05:26Z — Execution mode: no orchestrator-provided answered-questions file exists on disk for this unit and no prior nfr-design question phase is evidenced. Ran effectively in ARTIFACT-ONLY mode, resolving the design questions by inference from this unit's on-disk nfr-requirements (performance/security/scalability/reliability/tech-stack) and functional-design business-logic-model, plus the sibling unit-notifications nfr-design pattern (closest analog: non-blocking, choreography-side, reuses the notification send seam) — rather than blocking with ask_question. Matches the precedent set by this unit's functional-design and the completed unit-notifications nfr-design run.
- 2026-09-10T15:05:26Z — Treated the ledger as the single "Critical-to-this-unit" dependency in the degradation model (fail-safe toward not-spamming), and every other dependency as Important (degrade-to-recorded-value). This follows directly from reliability-requirements REL-SLA-4/REL-SLA-11 and the at-most-once invariant; made it explicit across reliability-design, security-design, and logical-components so the fail-safe bias is unambiguous for infrastructure-design.

## Deviations
- 2026-09-10T15:05:26Z — The runtime's methodology MCP tools (create_artifact, link_artifacts, send_output, collect_metric, get_artifact, get_intent_graph, etc.) are NOT present in this session's actual tool set. The output contract asks to record each artifact via create_artifact; that tool cannot be invoked. Following the precedent documented in the sibling unit-notifications nfr-design memory.md (and consistent with the on-disk functional-design / nfr-requirements for this very unit), wrote the five nfr-design artifacts to the working tree under aidlc-docs/construction/unit-sla-escalation/nfr-design/ as the only available persistence, and surfaced the tool-availability gap explicitly rather than silently producing nothing.
- 2026-09-10T15:05:26Z — Step 7 (update aidlc-state.md) skipped: no aidlc-state.md exists in the working tree (state is runtime-owned per the execution environment, which instructs ignoring state/audit bookkeeping). Recorded the completion intent here instead of fabricating a state file.

## Tradeoffs
- 2026-09-10T15:05:26Z — Kept caching deliberately minimal: cache only the immutable EscalationPolicy for the tick lifetime; explicitly do NOT cache the pending-request view or hasFired lookups. Rationale: eligibility must be derived freshly each tick from live workflow state (self-healing, REL-SLA-5) and a stale hasFired risks a duplicate nudge — correctness over speed. Chose this over a broader read-through cache that would have shaved I/O but risked firing against advanced/withdrawn requests.
- 2026-09-10T15:05:26Z — Chose "no new resilience machinery" — the unit reuses the unit-notifications retry/circuit-breaker/DLQ seam rather than defining its own, and relies on the ledger dedupe key + catch-up (not distributed locking / leader election) for correctness. This matches tech-stack-decisions (no new transport, idempotency-not-locking) and minimizes coupling, at the cost of the SLA unit inheriting whatever transport SLO the notification unit provides (documented as inherited, not re-specified).
- 2026-09-10T15:05:26Z — Framed the scaling story as "single stateless scanner, scale by cadence before instances," with partition-by-department fan-out designed-in but not activated at MVP. Preferred over specifying an always-on multi-instance topology, since dispatch volume is bounded by threshold-arrival rate (not backlog) and the ledger dedupe makes fan-out safe whenever it is later needed.

## Open questions
- 2026-09-10T15:05:26Z — Confirm the scan cadence and the deployed scheduler binding (cron / EventBridge Scheduler) with infrastructure-design; carried the 15-min placeholder through all five artifacts.
- 2026-09-10T15:05:26Z — Confirm concrete SLA thresholds per stage/tier from product/HR (illustrative defaults TeamLead 24h/48h, HR 48h/96h) — they set the cadence-vs-threshold ratio that keeps tick cost negligible.
- 2026-09-10T15:05:26Z — Confirm the durable reminder-ledger store technology and its single-key hasFired read (p95 ≤ 20 ms budget) + append characteristics, its at-rest encryption binding, and its operational (not 7-year audit) retention/backup horizon — jointly with infrastructure-design.
- 2026-09-10T15:05:26Z — Confirm the concrete escalation-target policy (escalationContactResolver configuration) so audience widening stays a reviewed config change (SEC-DES-3), and confirm the missed-cadence alerting for the tick-liveness SLO (a silently dead scanner is the main reliability risk).
